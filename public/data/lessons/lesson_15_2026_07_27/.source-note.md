The two SVG files in `photos/` are faithful text-preserving reconstructions of the supplied lesson images. They were stored as UTF-8 SVG assets so the connected GitHub action could commit them safely without corrupting binary image data.
